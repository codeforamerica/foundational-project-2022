<h1>Submitted Applications</h1>

<ul>
  <% @benefits_apps.each do |app| %>
    <li>
      <%= link_to app.id, app %>
    </li>
  <% end %>
</ul>

<%= form_with model: @benefits_app do |form| %>
  <div>
    <%= form.submit "New Application"%>
  </div>
<% end %>